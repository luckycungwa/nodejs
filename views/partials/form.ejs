
<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title text-center mb-4 ">CREATE PROFILE</h5>
            <form action="/login" method="POST" id="login">
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="text" class="form-control" id="email" placeholder="Enter your email" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
        </div>
    </div>
</div>

<!-- <script>
    const form = document.getElementById("loginForm");
    // const output = document.getElementById("output");

    // HANDLE FORM SUBMISSION
    form.addEventListener("submit", async (event) => {
        event.preventDefault();
        const formData = new FormData(form);
        const response = await fetch("/login", {
            method: "POST",
            body: JSON.stringify(Object.fromEntries(formData)),
            headers: {
                "Content-Type": "application/json",
            },
        });

        if (response.ok) {
            console.log ("Welcome to profile")
            // const data = await response.json();
            // output.innerHTML = `<p>User images: ${data.images}</p><p>User videos: ${data.videos}</p>`;
        } else {
            console.log("LOGIN FAILED")
            // output.innerHTML = "<p>Login failed</p>";
        }
    });
</script> -->

<!-- INSTALL CODE npm install express ejs nodemon morgan -->
